name: text_recognition_project
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  http: ^0.12.2
  http_parser: ^3.1.4
  image_picker: ^0.6.7+14
  image_cropper: ^1.3.0
  camera:
  path_provider:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
